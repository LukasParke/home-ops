---
apiVersion: v1
kind: Service
metadata:
  name: blog
  labels:
    app.kubernetes.io/name: blog
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: blog
  ports:
    - name: http
      port: 80
      targetPort: 3000
